{% render "../../templates/license_header_scss.liquid",
		project     : project.attributes.spa_frontend_project_name,
  		description : project.attributes.spa_frontend_project_description,
		authors     : project.attributes.authors,
		copyright   : project.attributes.copyright,
		filename    : 'Header.scss'
		%}
$screen-width-limit-medium: 600px !default;

body > header {
	padding:0.5rem;
	display: grid;
	grid-template-areas: 
		"logo title    account"
		"logo subtitle account";
	grid-template-columns: auto 1fr;
	background-color: var(--header-background-color);
	color: var(--header-text-color);

	.logo {
		grid-area: logo;
		width: 150px;
		margin-right:1rem;
		background-color: var(--header-text-color); /* logo color */
		filter: invert(1);
	}

	.title {
		grid-area: title;
		font-size: 1.8rem;
		font-style: italic;
	}

	.subtitle {
		grid-area: subtitle;
		font-size: 1.3rem;
		font-style: italic;
		color:lightBlue;
	}

	.account-button {
		grid-area: account;
		filter: invert(1);
	}

}
